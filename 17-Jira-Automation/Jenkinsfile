@Library('jiraLib') _

pipeline {
    agent any

    stages {
        stage('Jira Ticket Creation') {
            steps {
                script {
                    // Call the function from your shared library
                    createJiraTicket()
                }
            }
        }
    }
}
